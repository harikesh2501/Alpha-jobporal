// backend/routes/applicationRoutes.js
const express = require('express');
const router = express.Router();
// No routes here as they are nested under jobs and admin for context

module.exports = router;